services:
  mt5:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mt5-workbench
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./mt5-data/.wine:/home/abc/.wine
      - ./mt5-data/audio:/home/abc/audio
    environment:
      - DISPLAY=:1
